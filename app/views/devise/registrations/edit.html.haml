- content_for(:title, 'Edit Account')
.row
  .span2
    = render :partial => "shared/account_sidebar"
  .span9
    .title-area
      %h1 Edit Profile
    .area
      = simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put, :class => 'form-horizontal' }) do |f|
        = f.error_notification
        .control-group= f.input :firstname, :required => true, :autofocus => true, :label => 'First Name'
        .control-group= f.input :lastname, :required => true, :label => 'Last Name'
        .control-group= f.input :email
        .control-group= f.input :password, :hint => "Leave it blank if you don't want to change it"
        .control-group= f.input :password_confirmation
        .control-group= f.input :current_password, :hint => "We need your current password to confirm your changes"
        = f.button :submit, "Update", :class => 'btn btn-primary'
        = link_to "Back", accounts_path, :class => 'btn'
    .title-area
      %h1 Cancel my account
    .area
      = link_to "Cancel my account", registration_path(resource_name), :confirm => "Are you sure?", :method => :delete, :class => 'btn btn-danger'
